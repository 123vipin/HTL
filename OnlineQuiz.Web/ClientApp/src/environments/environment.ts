// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`. Host: 'http://localhost:63681'


export const environment = {
  production: true,
  //url: 'http://api.KaysthaMatrimoneySite.com'
    //readonly rootUrl = 'http://api.KaysthaMatrimoneySite.com';
  //readonly rootUrl = 'http://192.168.2.9:8023';
  url: 'http://localhost:52395'

}
